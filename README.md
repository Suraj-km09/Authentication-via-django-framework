# Authentication-via-django-framework
This project is a fully functional Django Authentication System that includes user registration, login, logout, and a protected dashboard. It uses Django’s built-in authentication framework along with custom views, forms, and Bootstrap-styled templates. The system handles secure password hashing, CSRF protection, session management,.
